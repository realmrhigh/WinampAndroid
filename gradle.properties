# For AndroidX compatibility
android.useAndroidX=true
android.enableJetifier=true

# Default JVM arguments for Gradle. Adjust Xmx if needed based on your RAM.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Optional: Enable parallel builds (uncomment if you have multiple cores)
# org.gradle.parallel=true

# Optional: Enable Gradle build cache (uncomment for faster incremental builds)
# org.gradle.caching=true